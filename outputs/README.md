# Outputs

Training and evaluation scripts populate `figs/` and `tables/` with plots, reports, and diagnostic tables.

These folders are kept empty in version control to avoid committing large artefacts; run the pipeline to regenerate the assets when needed.
